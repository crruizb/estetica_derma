---
interface Props {
  src: string
  title: string
  items: string[]
}

const {src, title, items} = Astro.props
---
  <div class="flex flex-col ">
    <div class="flex flex-col items-center">
      <img src={src} alt="" width="64px">
      <h2 class="text-4xl font-thin my-6">{title}</h2>
    </div>
    <ul class="list-disc">
      {
        items.map((item) => <li class="font-inter font-light">{item}</li>)
      }
    </ul>
  </div>